'use strict';

describe('Service: firebaseUtil', function () {

  // load the service's module
  beforeEach(module('batchUtilApp'));

  // instantiate service
  var firebaseUtil;
  beforeEach(inject(function (_firebaseUtil_) {
    firebaseUtil = _firebaseUtil_;
  }));

  /*it('should do something', function () {
    expect(!!firebaseUtil).toBe(true);
  });*/

});
